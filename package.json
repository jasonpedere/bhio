{
  "name": "bhio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && wrangler deploy"
  },
  "devDependencies": {
    "playwright-core": "^1.62.1",
    "vite": "^7.1.5",
    "wrangler": "^4.0.0"
  }
}
